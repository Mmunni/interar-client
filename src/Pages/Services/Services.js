import React from 'react';
import useTitle from '../../hooks/useTitle';

const Services = () => {
    useTitle('services');
    return (
        <div>
            <h1>services</h1>
        </div>
    );
};

export default Services;